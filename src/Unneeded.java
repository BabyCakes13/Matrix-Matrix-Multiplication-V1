package multiplication;


public class Unneeded {
	/*private float[][] matrix;
	
	public MatrixInitaliser(float[][] matrix) {
		this.matrix = matrix;
	} 
	
	public Cell[][] createCellMatrix() {
		int size = this.matrix.length;
		Cell[][] cellMatrix = new Cell[size][size];
		
		for(int i = 0; i < size; ++i) {
			for(int j = 0; j < size; ++j) {
				float sum = this.matrix[i][j];
				cellMatrix[i][j] = new Cell(sum);
			}
		}
		
		return cellMatrix;
	}*/
}
